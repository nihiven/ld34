game mechanics
 - how to control
  : slow dot
  : fast dot

add background to in game menu
change display of / and *





ok to use: local menu = menu




				if (oper == '/') then -- no fractions
					op2, ans = math.random(1,9), math.random(1,9)
					op1 = op2 * ans
				else
					op1, op2 = math.random(1,9), math.random(1,9)

					if (op1 < op2 and oper == '-') then -- no negative answers
						op1, op2 = op2, op1
					end
